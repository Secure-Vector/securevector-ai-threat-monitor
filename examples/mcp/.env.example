# SecureVector MCP Server Environment Variables
# Copy this file to .env and configure your settings

# API Key (optional - enables cloud/hybrid mode)
# If not set, server runs in local-only mode
# SECUREVECTOR_API_KEY=your_api_key_here

# Server Configuration
SECUREVECTOR_MCP_MODE=balanced          # Options: development, balanced, production
SECUREVECTOR_MCP_LOG_LEVEL=INFO         # Options: DEBUG, INFO, WARNING, ERROR
SECUREVECTOR_MCP_TRANSPORT=stdio        # Options: stdio, http, sse

# Server Network Settings (for HTTP/SSE transports)
SECUREVECTOR_MCP_HOST=localhost
SECUREVECTOR_MCP_PORT=8000

# Optional: Custom configuration file path
# SECUREVECTOR_CONFIG_PATH=/app/config

# Optional: Custom data directory
# SECUREVECTOR_DATA_PATH=/app/data
